---
title: Alley light
author: luis
excerpt: A gap between two buildings, shot into the sun at f/8 and pushed a stop and a third past what the meter asked for. Nine years ago today.
photo:
  src: "https://dlt23dunpqfwk.cloudfront.net/antfarm/alley-light/img_0455.jpg"
  alt: "A narrow alley between a dark gray cinder block wall and a red brick building with barred windows, the far end blown out to white by direct sun"
  width: 1621
  height: 1080
  exif: true
  camera: "Fujifilm X100F"
  lens: "23mm"
  settings: "f/8 &middot; 1/60 &middot; ISO 1600 &middot; +1⅓ EV"
  taken: 2017-09-03
---
{% include photo.html %}

{% comment %}
Draft scaffolding — Luis's words go here, then delete this block.

The photo, the front matter and the caption are wired up and verified live. What's
left is yours:

1. `title:` and `excerpt:` are placeholders in the wrong voice. The excerpt feeds
   `description`, `og:description` and `twitter:description`, so it needs to be real
   prose, ~160 chars.
2. No `image:` set, so the link card falls back to /images/ant.png. A CDN URL will
   NOT work there — card crawlers send no Referer and get a 403. If you want this
   photo on the card it needs a committed copy in /images/covers/.
3. No `date:` — it comes from the filename when you move this to `_posts/`, so the
   publish date is whenever you decide, not whenever the draft was written. Shot
   2017-09-03, if the anniversary is useful to you.

Everything in `photo:` above came out of `bin/prepare-photo`, read from the EXIF that
shipped with the file. The one thing the metadata can't tell you is the film
simulation — the Wi-Fi transfer app ate the maker notes back in 2017.
{% endcomment %}
