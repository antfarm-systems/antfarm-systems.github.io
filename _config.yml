# ----------- CONFIGURABLE START -----------
title:            "AntFarm Systems"
description:      "This is an opinion site."
url:              https://antfarm.systems # Site url, without a slash at the end.
signoff:          "The AntFarm" # Fallback byline; per-post bylines live in _data/authors.yml
facebook_appid:   # APP ID for like button, remove from /_includes/share.html if unwanted.
google_analytics: G-DMG7WF4SSG # Set tracking, remove from /javascripts/basic.js if unwanted.
share:            true # Turn on twitter and facebook share buttons

links:
  - name:         Posts
    url:          /
    external:     false
  - name:         Collections
    url:          /collections/
    external:     false
  - name:         About This Page
    url:          /about
    external:     false
# ------------ CONFIGURABLE END ------------

permalink:        /posts/:title
defaults:
  - values:
      layout: "default"
      author: "antfarm" # Byline key from _data/authors.yml. Use "luis" for posts you wrote.

include:
  - "*.txt"
exclude:
  - "vendor"
  - "CNAME"
  - "Gemfile*"
  - "README*"

plugins:
  - jekyll-sitemap
  - jemoji

whitelist:
  - jekyll-sitemap
  - jemoji

