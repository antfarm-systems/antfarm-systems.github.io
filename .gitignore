_site
.DS_Store
.jekyll
.jekyll-metadata
.bundle
.sass-cache
Gemfile.lock
node_modules
vendor

# Obsidian vault config (the vault is the repo root). Plugins and themes are
# ~7MB of machine-local state, not site content.
.obsidian/

# Leftover diagram-tool output, not referenced by any post.
generated-diagrams/

# Local working notes.
TODO.md

# bin/prepare-photo's staging area. The derivatives it writes live on the CDN;
# keeping copies here would put the megabytes back in the repo we sent them to
# S3 to avoid. Jekyll would also try to publish them.
tmp/
