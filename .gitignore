_site
.DS_Store
.jekyll
.jekyll-metadata
.bundle
.sass-cache
Gemfile.lock
node_modules
vendor

# Obsidian vault config (the vault is the repo root). Plugins and themes are
# ~7MB of machine-local state, not site content.
.obsidian/

# Leftover diagram-tool output, not referenced by any post.
generated-diagrams/

# Local working notes.
TODO.md
